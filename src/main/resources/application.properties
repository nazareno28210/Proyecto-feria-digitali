spring.application.name=FeriaDigital

server.port=${PORT:8080}

## Configuraciones de MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/feriadigital?useSSL=false&serverTimezone=UTC&sessionVariables=FOREIGN_KEY_CHECKS=0}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Cambiado a 'update' para evitar borrado de tablas en producción
spring.jpa.hibernate.ddl-auto=create
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

## Configuraciones de Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dklkf0fmq}
cloudinary.api-key=${CLOUDINARY_API_KEY:121724244342759}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:UoLQBsGv7hdXxA5W17GEX6sZAeU}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

## Configuraciones de Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:noreply.feriadigital@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eeegyvpgonbszoqj}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

## Configuración de URL base para enlaces en emails (Ej: Ngrok / Producción / Local)
app.base-url=${APP_BASE_URL:https://snowy-chop-dismiss.ngrok-free.dev}
